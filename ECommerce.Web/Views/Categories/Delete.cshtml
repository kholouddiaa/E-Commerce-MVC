@model CategoryDto
@{
    ViewData["Title"] = "Archive Category";
}

<div class="page-header">
    <div>
        <h1>Archive Category</h1>
        <p class="text-muted mb-0">Confirm before archiving this category.</p>
    </div>
    <a asp-action="Admin" class="btn btn-outline-secondary">Back to List</a>
</div>

<div class="card border-danger card-shadow">
    <div class="card-body">
        <div class="alert alert-warning">
            This action will archive <strong>@Model.Name</strong> if it has no active related products.
            It will no longer appear in normal category lists.
        </div>

        <dl class="row">
            <dt class="col-sm-3">Description</dt>
            <dd class="col-sm-9">@(string.IsNullOrWhiteSpace(Model.Description) ? "No description provided." : Model.Description)</dd>
        </dl>

        <form asp-action="Delete" method="post">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger">Archive</button>
            <a asp-action="Admin" class="btn btn-outline-secondary">Cancel</a>
        </form>
    </div>
</div>
