@model ProductFormViewModel
@{
    ViewData["Title"] = "Edit Product";
}

<div class="page-header">
    <div>
        <h1>Edit Product</h1>
        <p class="text-muted mb-0">Update product details through the business service.</p>
    </div>
    <a asp-action="Admin" class="btn btn-outline-secondary">Back to List</a>
</div>

<div class="card card-shadow border-0">
    <div class="card-body">
        <form asp-action="Edit" method="post" enctype="multipart/form-data">
            <partial name="_Form" model="Model" />
            <button type="submit" class="btn btn-primary" disabled="@(!Model.HasCategories)">Update</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
